class Runner
  def initialize: (Config config) -> void
  def run: () -> untyped  # The return type depends on the implementation of `chat_completion` in `Langchain::LLM::OpenAI`

  private

  attr_reader config: Config
end